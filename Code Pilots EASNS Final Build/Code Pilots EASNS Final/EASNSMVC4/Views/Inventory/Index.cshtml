@model IEnumerable<EASNSMVC4.Models.Inventory>

@{
    ViewBag.Title = "Inventory";
}
@if(User.IsInRole("Admin"))
{
    <h1 id="disTitle" class="headerText">Inventory</h1>
    <p>@Html.ActionLink("Back to Control Pannel", "Index", "Admin")</p>

    <p>
        @Html.ActionLink("Create new inventory", "Create")
    </p>
    <table>
        <tr style="border-bottom: solid 1px black;">
            <th>
                @Html.DisplayNameFor(model => model.InventoryType.Name)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr style="border-bottom: solid 1px black;">
            <td>
                @Html.DisplayFor(modelItem => item.InventoryType.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) 
                @Html.ActionLink("Details", "Details", new { id=item.ID }) 
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }

    </table>
}